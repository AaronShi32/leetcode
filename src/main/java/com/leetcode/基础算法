------------ 常用算法时间复杂度 ---------------

二分搜索: O(logn)

堆: 建堆 O(n), 排序 O(nlogn) 不稳定排序

快排: O(nlogn)

归并排序: O(nlogn)

前缀树(Trie Tree): 建树 O(n * le) le 是单词的平均长度


-------------基础: 时间复杂度解释 ---------------

指明某个算法的耗时/耗空间与数据增长量之间的关系。其中的 n 代表输入数据的量

关系: O(1) < O(logn) < O(n) < O(nlogn) < O(n2)

举例: 当 n = 256 时

1 < 8 < 256 < 8 * 256 < 256 * 256